# 严格的工具调用限制规则 (Strict Tool Calling Constraints)
1. 默认禁用策略：在回答纯理论问题、算法设计、基础语法讲解或面试题准备时，严禁调用 `Glob`、`Grep`、`ReadFile`、`LS` 等任何本地工具。
2. 显式授权机制：只有当我的问题需求中要求了“读取项目”、“分析当前目录”、“修改代码”等指令时，或者不读取就无法完成时，才允许你使用文件系统工具。
3. 最小范围原则：如果我指定了读取某个特定文件（如 `cat script.py`），你只能读取该文件，绝对禁止为了寻找上下文而自发扫描其他未被提及的目录。
